{
  "name": "gitcoin",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ santteegt/gitcoin",
    "create-local": "graph create --node http://localhost:8020/ santteegt/gitcoin",
    "remove-local": "graph remove --node http://localhost:8020/ santteegt/gitcoin",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 santteegt/gitcoin"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.12.0",
    "@graphprotocol/graph-ts": "0.11.0"
  }
}
